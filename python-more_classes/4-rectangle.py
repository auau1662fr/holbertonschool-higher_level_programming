#!/usr/bin/python3
"""Rectangle class with eval support."""


class Rectangle(Rectangle):
    """Rectangle can be recreated using eval(repr(rectangle))."""
    pass  # hérite de 3-rectangle, repr() déjà défini
